name: api
description: An Dart Frog api backend for Qchat app.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.0.0
  envied: ^0.5.2

dev_dependencies:
  build_runner: ^2.4.7
  envied_generator: ^0.5.2
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0